{
  "name": "noizy",
  "version": "1.0.0",
  "private": true,
  "description": "Tests for the Noizy Chrome extension",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:static": "node --test tests/static.test.js",
    "test:e2e": "node --test tests/e2e.test.js"
  },
  "devDependencies": {
    "puppeteer-core": "^24.0.0"
  }
}
